public:

version (WhiteTriangle) {
    import whiteTriangle.entry;
}

version (PerVertexTriangle) {
    import perVertexTriangle.entry;
}

version (UniformTriangle) {
    import uniformTriangle.entry;
}

version (TextureRectangle) {
    import textureRectangle.entry;
}
